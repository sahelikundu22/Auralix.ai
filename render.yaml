services:
  - type: web
    name: syncmaster-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
